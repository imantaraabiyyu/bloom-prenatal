# CLAUDE.md

<!-- beehive:improver-skill-auto-invoke -->
## Improvement requests

When asked to improve, extend, or fix behavior in an existing feature in this codebase, use the `improver` skill (via the Skill tool) rather than implementing directly — it assesses the code, clarifies ambiguity, gets plan approval, then implements with tests and a local commit.
